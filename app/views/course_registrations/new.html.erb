hi!

<%= form_for [@student, @registration_roster] do |f| %>

  <%= f.label :first %>

  <%= f.check_box(:first_course) %>
  <%= f.label(:first_course, "Lead", value: "Lead") %>
  <%= f.check_box(:first_course) %>
  <%= f.label(:first_course, "Follow", value: "Follow") %>

<%= f.submit 'Register for these classes' %>
<% end %>
